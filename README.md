# 📊 Log Anomaly Detection App

## 📌 Опис проєкту

Даний проєкт представляє собою програмне забезпечення для аналізу лог-файлів та виявлення аномалій із використанням алгоритмів машинного навчання.

Програма дозволяє користувачу завантажити лог-файл, виконати його аналіз та отримати результати у вигляді таблиці, графіка та звіту.

---

## ⚙️ Використані технології

- Python  
- Tkinter (графічний інтерфейс)  
- pandas (обробка даних)  
- scikit-learn (алгоритм Isolation Forest)  
- matplotlib (побудова графіків)  

---

## 🚀 Як запустити програму

### 1. Встановити Python (якщо не встановлений)

### 2. Встановити необхідні бібліотеки

```bash
python -m pip install pandas scikit-learn matplotlib

Fix
	Додано обробку відсутності файлу конфігурації (config.json) з використанням try/except 
	Реалізовано обробку помилки JSONDecodeError для некоректного формату конфігураційного файлу 
	Додано перевірку порожніх або некоректних лог-файлів перед аналізом 
	Реалізовано обробку виняткових ситуацій при відсутності або некоректному вхідному файлі (FileNotFoundError) 
	Додано валідацію даних перед їх обробкою (перевірка df.empty) 
	Змінено логіку доступу до даних (row.get()), що запобігає помилкам при відсутності полів 
	Винесено обробку різних типів помилок (ValueError, FileNotFoundError) для більш точного інформування користувача 
	Забезпечено стабільну роботу програми навіть при неочікуваних помилках за допомогою загального обробника виключень 
	Покращено користувацький інтерфейс повідомленнями про помилки через messagebox

